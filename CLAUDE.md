# npb-pennant

NPB セ・パ両リーグの**優勝確率**と**優勝が決まる日**を毎日計算して公開する静的サイト。

- 設計と計算ロジックの詳細 → [docs/SPEC.md](docs/SPEC.md)
- コマンド一覧 → [README.md](README.md)

## 現状（2026-09-07 時点）

**公開済み → https://yoshi-asas.github.io/npb-pennant/**

| 項目                          | 状態                                                    |
| ----------------------------- | ------------------------------------------------------- |
| 計算エンジン（`src/engine/`） | 完成。Vitest 51件パス                                   |
| データ取得・検証              | 完成。NPB 公式順位表と12球団完全一致を確認済み          |
| サイト（17ページ）            | 完成。ビルド通る                                        |
| もしもシミュレーター          | 完成。動作確認済み                                      |
| GitHub Actions                | push 経路は成功。**取得〜自動コミットは未検証**         |
| git                           | `yoshi-asas/npb-pennant`（Public）。`main` を push 済み |

最新の数字は `data/2026/latest.json` を見ること。CI が毎日書き換えるので、ここに
書くとすぐ古くなる。2026年シーズンはセが阪神、パがソフトバンクの優勝濃厚という情勢。

## 次にやること（優先順）

1. **`workflow_dispatch` で手動実行し、スクレイプ〜データ自動コミットを確認する。**
   初回の実行は push 起動だったため、取得・照合・コミットの3ステップがスキップ
   されている。`daily.yml` は push 起動時にそこを飛ばす作りなので、毎晩の自動更新の
   中核は**まだ一度も動いていない**。bot の書き込み権限が実際に効くかもここで判明する
2. 余力があれば docs/SPEC.md §11 の拡張候補（優勝確率の推移グラフ、CS・日本シリーズ確率、2025年シーズンでの通し検証）

なお `gh` コマンドはこの環境に入っていない。Public リポジトリなので、状態確認は
公開 API を `curl` で叩けば認証なしでできる。

## 日々の運用

```bash
npm run update    # scrape → verify → snapshot
```

`verify` が NPB 公式順位表と照合し、1件でも食い違えば落ちる。落ちれば `snapshot` は走らないので、壊れたデータが `latest.json` に入ることはない。

**npb.jp への確定スコア反映は試合終了から数時間かかる。** 9/4 0:20 の時点で 9/3 の結果がまだ載っていなかった。その日の試合を確実に取り込むなら翌朝以降に実行する。

## ハマりどころ（実際に踏んだもの）

- **GitHub への push はパスワード認証が使えない**（2021年に廃止）。`workflow` スコープ付きの
  Personal Access Token をパスワード欄に貼る。`repo` だけだと `.github/workflows/` の
  push が拒否される
- **PowerShell で `npm` が実行ポリシーに弾かれる。** `npm.cmd run dev` と拡張子まで書けば通る。ユーザーの環境では `npm.ps1` がブロックされている
- **`npm run scrape -- --months 09` は既存データを消さない**（修正済み）。取得した月だけを差し替えるようにしてある
- **Svelte のプロキシは `postMessage` で複製できない。** ワーカーへ渡す前に `send()` で JSON 経由の素オブジェクトに落とす。詳細は SPEC §8-1
- **dev サーバーはユーザーが起動していることが多い。** ポート 4321。既に動いているなら `navigate` でそこへ繋げば画面もコンソールも見られる
- **日付は UTC で組み立てて UTC で読む。** `new Date('2026-09-22T00:00:00+09:00')` は
  前日の 15:00Z を指すので、そこから `getUTCDate()` を読むと常に1日早くなる。
  この取り違えで「優勝が決まる日」が全ページで1日ずれていた（修正済み、`format.test.ts` に回帰テストあり）。
  ローカル時刻を使わないのは CI（UTC）と手元（JST）で結果を変えないため
- **`.astro` のテンプレート式に `<=` を書くと `astro check` が落ちる。** `<` をフラグメント開始タグと誤読する。`astro build` は通ってしまうので気づきにくい。比較は frontmatter 側でやって、テンプレートには結果だけ渡す（`ClinchHistogram.astro` の `labelStep` がその例）

## 設計上、壊してはいけないところ

- **`src/engine/compare.ts` が順位決定規定の唯一の実装。** 順位表・マジック・敗退判定・シミュレーションが全部ここを経由する。ここを変えると全指標が同時に動くので、変更時はテストを厚くする
- **残り試合は日程表から数えず `25 −（消化数）` で導出する。** 日程表を数えると雨天中止で再編成待ちの試合を取りこぼす
- **サイト内リンクは必ず `src/lib/url.ts` の `url()` を通す。** GitHub Pages の
  プロジェクトページではサイトが `/npb-pennant/` 配下に置かれ、`href="/method/"` と
  直書きすると 404 になる。Astro は直書きの絶対パスを書き換えてくれない。
  base は `astro.config.mjs` が `BASE_PATH` から読み、daily.yml が
  configure-pages の `base_path` を渡す。ローカルは `/` なので素通しになり、
  **直書きしても手元では気づけない**
- **マジックの判定に最大流は使わない。** ライバル単独で全勝すれば追い抜けるので潰し合いを考える必要がない。最大流が要るのは敗退判定だけ。詳細は SPEC §6-2

## 書き方の約束

- コメント・UIテキスト・仕様書はすべて日本語
- Prettier: タブ・シングルクォート・末尾カンマなし・100桁
- 設計の判断理由と、却下した案は docs/SPEC.md に残す。ハマった落とし穴も「記録」として書く
