# npb-pennant

NPB セ・パ両リーグの**優勝確率**と**優勝が決まる日**を毎日計算して公開する静的サイト。

- 設計と計算ロジックの詳細 → [docs/SPEC.md](docs/SPEC.md)
- コマンド一覧 → [README.md](README.md)

## 現状（2026-09-05 時点）

**動くものは一通り揃っている。まだ1度もコミットしていない。**

| 項目                          | 状態                                                         |
| ----------------------------- | ------------------------------------------------------------ |
| 計算エンジン（`src/engine/`） | 完成。Vitest 36件パス                                        |
| データ取得・検証              | 完成。NPB 公式順位表と12球団完全一致を確認済み               |
| サイト（17ページ）            | 完成。ビルド通る                                             |
| もしもシミュレーター          | 完成。動作確認済み                                           |
| GitHub Actions                | 書いてあるが**未検証**（リポジトリが無いので走らせていない） |
| git                           | `master` ブランチに**コミット0件**。未追跡15件               |

最新データは 2026-09-03 終了時点。阪神 96.3%（M18、決定日中央値 9/25）、ソフトバンク 96.5%（M18、9/23）。

## 次にやること（優先順）

1. **初回コミット**（まだ1つもない）
2. **GitHub にリポジトリを作って push** → Settings → Pages → Source を「GitHub Actions」に変更 → 公開
3. `.github/workflows/daily.yml` を `workflow_dispatch` で手動実行して、スクレイプ〜デプロイが通るか確認
4. 余力があれば docs/SPEC.md §11 の拡張候補（優勝確率の推移グラフ、CS・日本シリーズ確率、2025年シーズンでの通し検証）

## 日々の運用

```bash
npm run update    # scrape → verify → snapshot
```

`verify` が NPB 公式順位表と照合し、1件でも食い違えば落ちる。落ちれば `snapshot` は走らないので、壊れたデータが `latest.json` に入ることはない。

**npb.jp への確定スコア反映は試合終了から数時間かかる。** 9/4 0:20 の時点で 9/3 の結果がまだ載っていなかった。その日の試合を確実に取り込むなら翌朝以降に実行する。

## ハマりどころ（実際に踏んだもの）

- **PowerShell で `npm` が実行ポリシーに弾かれる。** `npm.cmd run dev` と拡張子まで書けば通る。ユーザーの環境では `npm.ps1` がブロックされている
- **`npm run scrape -- --months 09` は既存データを消さない**（修正済み）。取得した月だけを差し替えるようにしてある
- **Svelte のプロキシは `postMessage` で複製できない。** ワーカーへ渡す前に `send()` で JSON 経由の素オブジェクトに落とす。詳細は SPEC §8-1
- **dev サーバーはユーザーが起動していることが多い。** ポート 4321。既に動いているなら `navigate` でそこへ繋げば画面もコンソールも見られる

## 設計上、壊してはいけないところ

- **`src/engine/compare.ts` が順位決定規定の唯一の実装。** 順位表・マジック・敗退判定・シミュレーションが全部ここを経由する。ここを変えると全指標が同時に動くので、変更時はテストを厚くする
- **残り試合は日程表から数えず `25 −（消化数）` で導出する。** 日程表を数えると雨天中止で再編成待ちの試合を取りこぼす
- **マジックの判定に最大流は使わない。** ライバル単独で全勝すれば追い抜けるので潰し合いを考える必要がない。最大流が要るのは敗退判定だけ。詳細は SPEC §6-2

## 書き方の約束

- コメント・UIテキスト・仕様書はすべて日本語
- Prettier: タブ・シングルクォート・末尾カンマなし・100桁
- 設計の判断理由と、却下した案は docs/SPEC.md に残す。ハマった落とし穴も「記録」として書く
